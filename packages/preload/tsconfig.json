{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Electron sandboxed preloads are loaded as CommonJS, so this package must not emit ESM.
    "module": "CommonJS",
    "moduleResolution": "Node",
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"]
}
